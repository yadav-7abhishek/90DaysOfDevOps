1. We can check out present working directory with `pwd` command.
2. We can list all the files or directories including hidden files by
           `ls -la`
3. We can created nested directory by `mkdir -p A/B/C/D`
